{
  "_comment": "文本回复卡片 — 替换 {{TITLE}}、{{COLOR}}、{{CONTENT}}",
  "schema": "2.0",
  "config": {
    "wide_screen_mode": true
  },
  "header": {
    "title": {
      "tag": "plain_text",
      "content": "{{TITLE}}"
    },
    "template": "{{COLOR}}"
  },
  "body": {
    "elements": [
      {
        "tag": "markdown",
        "content": "{{CONTENT}}"
      }
    ]
  }
}
